{
  "name": "@example/minimal",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "img": "node --no-experimental-fetch ./src/utilities/img.js",
    "dev": "node --no-experimental-fetch ./src/utilities/img.js && astro dev",
    "test": "astro build && npx playwright test",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro-accelerator": "^0.0.25"
  }
}
